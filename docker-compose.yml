services:
  postgres:
    image: postgres:16-alpine
    container_name: pharma-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: pharma
      POSTGRES_PASSWORD: pharma_secret
      POSTGRES_DB: pharma_delivery
    ports:
      - "${POSTGRES_HOST_PORT:-5433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pharma -d pharma_delivery"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: pharma-redis
    restart: unless-stopped
    ports:
      - "${REDIS_HOST_PORT:-6380}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # NO usar en el servidor Windows compartido (chocan puertos 4000/5173).
  # LAN: solo postgres+redis + npm run dev:backend / dev:web en 4410/5517.
  # Activar solo con: docker compose --profile full-stack up -d
  backend:
    profiles: ["full-stack"]
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    container_name: pharma-backend
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: production
      PORT: 4000
      DATABASE_URL: postgresql://pharma:pharma_secret@postgres:5432/pharma_delivery?schema=public
      REDIS_URL: redis://redis:6379
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:-your-super-secret-access-key-min-32-chars!!}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-your-super-secret-refresh-key-min-32-chars!}
      CORS_ORIGIN: http://localhost:5173,http://192.168.20.26:5173,http://localhost:80
      UPLOAD_DIR: /app/uploads
    volumes:
      - uploads_data:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  web-admin:
    profiles: ["full-stack"]
    build:
      context: .
      dockerfile: apps/web-admin/Dockerfile
      args:
        VITE_API_URL: http://localhost:4000
    container_name: pharma-web-admin
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  postgres_data:
  redis_data:
  uploads_data:
