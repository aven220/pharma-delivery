# WEB_API_URL — URL HTTPS pública del API (sin puertos internos).
# En Docker producción se pasa como VITE_API_URL en el build.
#
# Desarrollo: dejar vacío (proxy Vite → localhost:4000).
# Producción: https://tu-dominio-o-ip
#
VITE_API_URL=
