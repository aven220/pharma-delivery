import type { Server } from 'socket.io';
export declare function createIncidentRoutes(io?: Server): import("express-serve-static-core").Router;
export declare function createEvidenceRoutes(): import("express-serve-static-core").Router;
