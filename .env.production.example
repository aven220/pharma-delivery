# =============================================================================
# A-AS Delivery — Variables de entorno PRODUCCIÓN
# Copiar y editar en el servidor:
#   cp .env.production.example .env.production
#   nano .env.production
#
# IMPORTANTE: generar secretos únicos:
#   openssl rand -base64 48
# =============================================================================

# --- PostgreSQL ---
POSTGRES_USER=pharma
POSTGRES_PASSWORD=CAMBIAR_clave_postgres_segura_min_16_chars
POSTGRES_DB=pharma_delivery

# --- Puertos publicados ---
PUBLIC_PORT=8080
WEB_PORT=8081

# --- JWT (mínimo 32 caracteres cada uno, valores distintos) ---
JWT_ACCESS_SECRET=CAMBIAR_jwt_access_minimo_32_caracteres_unicos
JWT_REFRESH_SECRET=CAMBIAR_jwt_refresh_minimo_32_caracteres_unicos

# --- CORS (URLs del admin web, separadas por coma) ---
CORS_ORIGIN=http://20.5.19.8:8081

# --- URLs públicas ---
VITE_API_URL=http://20.5.19.8:8080
APP_PUBLIC_URL=http://20.5.19.8:8081

# --- Logs ---
LOG_LEVEL=info

# --- Backups ---
BACKUP_RETENTION_DAYS=14

# --- Correo (recuperación de contraseña) ---
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=aux.tecno.med@cencoic.com.co
SMTP_PASS=CAMBIAR_clave_smtp
SMTP_FROM="A-AS Delivery <aux.tecno.med@cencoic.com.co>"

# --- Push móvil Expo (opcional) ---
EXPO_ACCESS_TOKEN=
