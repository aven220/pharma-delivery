# A-AS Delivery — Producción (20.5.19.8)
# Generar secretos únicos si aún usa valores por defecto:
#   openssl rand -base64 48

# --- PostgreSQL ---
POSTGRES_USER=pharma
POSTGRES_PASSWORD=cambiar_por_clave_segura_larga
POSTGRES_DB=pharma_delivery

# --- Puertos publicados ---
PUBLIC_PORT=8080
WEB_PORT=8081

# --- JWT (mínimo 32 caracteres) ---
JWT_ACCESS_SECRET=cambiar_access_secret_minimo_32_caracteres
JWT_REFRESH_SECRET=cambiar_refresh_secret_minimo_32_caracteres

# --- CORS (URLs del admin web, separadas por coma) ---
CORS_ORIGIN=http://20.5.19.8:8081

# --- URLs públicas ---
VITE_API_URL=http://20.5.19.8:8080
APP_PUBLIC_URL=http://20.5.19.8:8081

# --- Logs ---
LOG_LEVEL=info

# --- Backups ---
BACKUP_RETENTION_DAYS=14

# --- Correo (recuperación de contraseña) ---
# Ajuste SMTP_HOST según su proveedor (ej. smtp.office365.com)
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=aux.tecno.med@cencoic.com.co
SMTP_PASS=1061740484A
SMTP_FROM="A-AS Delivery <aux.tecno.med@cencoic.com.co>"

# --- Push móvil Expo (opcional) ---
EXPO_ACCESS_TOKEN=
